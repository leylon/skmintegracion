<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SkmIntegracion"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BehaviorActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="icg.actions.electronicpayment.izipay.GET_BEHAVIOR"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".InitializeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="icg.actions.electronicpayment.izipay.INITIALIZE"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>

        </activity>
        <activity
            android:name=".GetVersionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="icg.actions.electronicpayment.izipay.GET_VERSION"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".TransactionActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="icg.actions.electronicpayment.izipay.TRANSACTION"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <activity android:name=".GetCustomParamsActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="icg.actions.electronicpayment.izipay.GET_CUSTOM_PARAMS"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>

        </activity>
    </application>

</manifest>